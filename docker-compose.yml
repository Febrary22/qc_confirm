services:
  qc-tool:
    build: .
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      # 사내 서버 등에서 여러 명이 함께 쓸 때, 아래 두 값을 채우면 접속 시 로그인 창(HTTP Basic Auth)이 뜹니다.
      # 비워두면(기본값) 인증 없이 누구나 접속할 수 있습니다.
      QC_BASIC_AUTH_USER: ""
      QC_BASIC_AUTH_PASS: ""
